# 变更记录

## 1.6.4 - 2026-09-08

- 修复重置后遗漏仍在追加内容的旧会话：历史扫描按日志内部事件时间判断窗口，文件修改时间不再作为排除依据。
- 增加读取器内存时间索引，未变化文件复用索引，追加记录按字节位置更新；截短、替换和同长度修改重建索引，未知时间交由正常扫描报告。
- 当前周与归档使用同一覆盖保护：同一或扩大的时间范围内，响应数、输入/输出或已有完整逐响应用量丢失时拒绝覆盖旧样本；正确的较早观察边界与用量不变的降价仍可生效。
- 完整区间拆分/合并整体校验覆盖，部分拆分不得误删剩余百分比。拒绝覆盖会在状态栏持续提示；重算后不再显示已被覆盖的实时金额。
- 当前周合并活动和归档目录；升级自动重建当前周与保留期历史，不修改模型价格。无法确认层级的区间保留旧记录并报告，不视为完整恢复。

## 1.6.3 - 2026-09-07

- 修正界面测试将北京时间写死的假设，按运行主机本地时区验证连接时间；不改变程序的本地时间显示逻辑。
- 接续 1.6.2 的 UI 优化与字体裁切修复，恢复非北京时间 Windows 构建环境的发布验证。

## 1.6.2 - 2026-09-07

- 根据实际生图优化稿调整主界面：深蓝主额度卡、青色金额、浅色辅助卡、更宽松的表格行距，以及深蓝底部状态栏与历史重算进度条。
- 修复卡片高度测量与 Windows 标签实际换行不一致导致的裁切；为中文、英文及不同字体增加窄卡片验证，保留原有高 DPI 布局测试。
- 诊断页明确提示生图费用尚未纳入估算。本次真实生图记录只有任务状态和产物，没有图像 token/usage 明细，不能仅添加模型单价就准确计费。
- 延续 1.6.1 的离线历史重算及旧曲线保留修复；不改变用户价格、样本或价格版本。

## 1.6.1 - 2026-09-07

- 修复保存新价格后旧曲线立即消失的问题：重算期间保留旧价格曲线并明确标注，最新结果完成后切换，避免新旧价格混合回归。
- 全局状态栏显示当前周与历史扫描进度；当前周优先处理。再次改价或退出会取消过时扫描，繁忙期间的最新查询请求会排队并立即续跑。
- 实时样本及历史重放样本保存逐响应模型、服务层级和 token 分类。完整明细支持无原始日志、无网络的离线改价重算，包括长上下文阈值变化。
- 升级后首次扫描补录旧样本明细；不能恢复的区间保留旧记录并明确报告。相同价格反复保存或复位不会重复追加同一区间样本。
- 修复旧窗口重放整段替换误删内部缺口样本的问题，仅替换成功重建的百分比区间；新增同一时间范围内有未覆盖区间的重复重放验证。
- 现有内置与自定义单价保持不变；本版本修复历史重算流程，不把 API 价格表当作 Codex 订阅收费承诺。

## 1.6.0 - 2026-09-07

### 模型价格设置

- 设置页新增“模型价格”分类，可逐模型修改短上下文与长上下文的未缓存输入、缓存读取、缓存写入和输出价格，以及长上下文阈值。
- 新增独立的“恢复内置价格”按钮；只复位价格编辑值，不影响轮询、数据源、回归、语言、主题或开机启动设置，保存后生效。
- 自定义价格随 `settings.json` 持久化；旧版设置首次升级时自动补齐当前程序内置价格。
- 每套自定义价格生成稳定且顺序无关的价格版本。价格变化后实时累计、当前周重放、旧窗口重放、图表和回归使用同一版本，避免新旧金额混算；恢复内置价格后回到内置版本。
- 内置价格已依据 2026-09-07 的 OpenAI Docs 重新核对，价格来源继续保留在设置说明与诊断信息中。

### 验证

- 70 项业务测试全部通过；新增旧设置价格迁移、自定义 Astra 缓存读取价格端到端计价、版本隔离、历史重算触发和价格专用复位场景。

## 1.5.8 - 2026-09-04

### 计费口径

- 新增 GPT-6 Astra（`gpt-6-astra`）Standard API 等价计价：短上下文输入、缓存读取、缓存写入、输出分别为 `$10/$1/$12.5/$50` 每百万 token。
- Astra 输入超过 272K 时，官方长上下文口径对整次请求应用 `$20/$2/$25/$75` 每百万 token；未开启长上下文对比时仍沿用短上下文基础口径。
- Astra 的 ChatGPT/Codex Fast 模式按官方 `2.5×` 额度倍率计算；该倍率与 API Fast 服务层级的 `2×` 价格保持明确区分。
- 价格观察日期更新为 2026-09-04，价格策略版本提升到 v5；升级后会自动重放当前周与保留期历史，以纳入此前无法定价的 Astra 响应。

### 验证

- 67 项业务测试全部通过；新增 Astra 短上下文 Standard、超过 272K 的官方长上下文，以及 Fast 额度倍率三类计价场景。

## 1.5.7 - 2026-09-03

### 修复

- 修复 App Server 同时返回多个等长周额度桶时，自动选择按 ID 排序而误选 `base_model_inference` 的问题；空配置现在明确优先选择 `codex` 主额度桶。
- “立即查询额度”和定时轮询共享修复后的选桶规则，显示百分比与 Codex 主界面的周额度保持同一口径；显式配置其他额度桶时仍尊重用户选择。

### 升级

- 安装继续原位覆盖固定安装路径，已有开始菜单快捷方式无需随版本修改；安装 v1.5.7 后原快捷方式会直接启动新版本。

### 验证

- 64 项业务测试全部通过；新增包含 `codex`、`base_model_inference` 和 `codex_bengalfox` 的真实多桶响应场景，并验证显式额度桶设置仍可覆盖默认优先级。

## 1.5.6 - 2026-09-02

### 修复

- 修复当前周历史重放质量暂时退化时会删除旧重放样本的问题；新结果现在只替换其成功覆盖的百分比区间，无法重建区间继续保留既有实时样本和历史重放样本。
- 当前周样本合并改为封闭区间单调保留：较少的新重放点不会让持久化样本数倒退，同一区间重新成功归因时仍会幂等替换旧结果。
- 当前周重放算法版本提升到 6，升级后会按新的单调合并语义重新执行一次。

### 验证

- 63 项业务测试全部通过；新增“17 个旧重放点退化为 7 个新结果”的场景，验证未覆盖的 10 个旧点保留、已覆盖区间替换及重复合入幂等。

## 1.5.5 - 2026-09-01

### 修复

- 修复当前周历史重放会删除无法重建区间内有效实时样本的问题；这会导致权威百分比持续增长、运行日志也曾报告“新增样本”，但最近 24 小时图表仍为空。
- 当前周重放现在始终刷新旧重放点，只替换本轮成功重建百分比区间所覆盖的实时样本，并保留未覆盖的实时样本供图表和回归继续使用。
- 当前周重放算法版本提升到 5，升级后会按新合并语义重新执行一次。

### 验证

- 62 项业务测试全部通过；当前周重放场景同时验证成功覆盖区间替换、未覆盖实时样本保留、旧重放点刷新和重复合入幂等性。

## 1.5.4 - 2026-08-28

### 口径与界面

- 所选时间范围现在先过滤原始样本，再重新执行回归或聚合；额度卡、趋势横幅、回归曲线和贡献计数统一消费同一次分析结果，不再把全历史曲线输出点误称为参与计算点。
- 贡献计数明确改为“当前估值贡献样本”：线性模式取最后 N 点，分段模式取最后时间窗口，高斯模式取全部有效样本并保留相对权重。
- 图表以琥珀色圆环标明当前估值贡献样本；高斯模式按权重调整圆环强弱，图例和悬停提示同步说明贡献含义与相对权重。
- 切换 24 小时、7 天、30 天或全部时，主卡金额、横幅金额、贡献数、曲线和图表标记同步更新；托盘估值仍使用完整保留期数据。

### 验证

- 62 项业务测试全部通过；新增三种算法贡献范围、30 天与全部范围重算、卡片/横幅/图表计数一致和贡献圆环绘制验证。

## 1.5.3 - 2026-08-28

### 修复与调整

- 系列按钮改为按当前字体和 DPI 测量完整文字宽度，并为扁平按钮边框预留空间，修复“基础样本折线”和“基础回归曲线”末字裁切。
- 选中的系列按钮使用实色系列色、白色文字和 2 像素边框；未选中项使用中性底色、弱化文字和 1 像素边框。
- 趋势图首次打开默认显示最近 30 天；用户在当前窗口手动改选后，语言或主题切换仍保留当前范围。

### 验证

- 60 项业务测试全部通过；新增 125% 内容缩放下的完整按钮文字、选中态差异和默认 30 天断言。

## 1.5.2 - 2026-08-28

### 修复

- 侧栏产品名按品牌区实际宽度约束并允许自然换行，品牌区由固定高度改为随内容增长，避免高缩放下右端文字被截断。
- 连接状态徽标改为按当前 DPI 和字体测量宽度，并使用紧凑 24 小时时间，移除依赖省略号隐藏溢出的行为。
- 有效样本说明压缩为“归档 / 待归因”紧凑口径，中英文在窄卡片内均完整显示。

### 验证

- 59 项业务测试全部通过；新增用户截图对应的 1261×773 客户区与 125% 内容缩放场景，直接断言产品名、连接徽标和样本说明不越界。
- 在用户提供的真实 Windows 高缩放截图中复核三处目标文字均已完整显示。

## 1.5.1 - 2026-08-27

### 修复

- 指标带由固定 116 像素改为按当前 DPI、字体首选高度和卡片内容自动增长，修复高缩放下说明文字被截断。
- 侧栏加宽并让表格列宽、表头高度和数据行高度使用实际文字测量，避免固定像素尺寸在高 DPI 下失效。
- 截图入口不再强制覆盖 Windows 原生窗口尺寸；DPI 测试会绑定真实业务数据并验证 100%、125%、150%、200% 下每张卡片和表格文字边界。

### 验证

- 58 项业务测试全部通过，并新增字体首选高度、标签客户区、表头和数据行的多缩放档位断言。

## 1.5.0 - 2026-08-27

### 界面

- 将总览、趋势图和逐点明细合并为同一个首屏工作台，移除重复的“历史与图表”一级页面与托盘入口。
- 首屏改为紧凑标题、五项指标带、趋势控制条、面积光晕曲线和轻量数据表，减少空白与厚重卡片。
- 侧栏缩减为“概览与趋势 / 设置 / 诊断”三个入口；系列开关和时间范围使用扁平控件。
- 修复主额度、重置时间和右端时间刻度在窄视口下的裁切，并取消表格首行默认高亮。

### 口径

- 官方 `>272K` 长上下文加价对比改为设置中的显式选项，默认关闭。
- 关闭时同时隐藏第二估值、系列开关、图例、表格列、快速详情和托盘摘要；底层样本与计价事实仍保留。
- 设置 schema 升级到 4，旧配置迁移后保持该选项关闭。

### 验证

- 业务测试增加到 58 项，覆盖默认关闭、显式启用、合并入口、高 DPI 布局和首屏趋势可见性。
- 在 1160×730 真实 WinForms 视口中完成三轮截图复核，并根据截图继续修正裁切、控件观感和视觉层级。

## 1.4.1 - 2026-08-27

### 性能

- rollout 读取改为 64 KiB 缓冲的 UTF-8 流式逐行处理，不再为单个大型 JSONL 同时分配整文件字节、完整字符串和行数组。
- 保留末尾半行的原字节游标语义；完整行、半行等待、文件截断恢复和多窗口回填口径不变。

### 验证

- 57 项业务测试全部通过，并继续覆盖末尾半行、截断重读、畸形行隔离和活动/归档多窗口重建。

## 1.4.0 - 2026-08-26

### 新增

- 历史重放从“仅当前周”扩展为图表保留期内的多窗口重建，自动合并 `.codex\sessions` 与同级 `.codex\archived_sessions`。
- 旧窗口按 `resets_at` 一分钟容差聚类，要求百分比严格递增，并在下一个重置簇首次出现时截断旧窗口，避免跨重置重复归因。
- 活动与归档中同名 rollout 只读取较完整副本；重复额度快照按百分比、窗口和重置承诺保留首次观察点。
- 诊断页显示旧窗口数量、旧样本数、扫描文件数和未归因区间数。

### 性能与安全

- 多窗口扫描在后台线程执行，只在算法、价格、历史保留天数或自动发现的数据根变化时重跑；一分钟轮询不会反复全盘扫描。
- 旧窗口缺少模型、服务层级、响应或第二个百分比点时继续失败关闭，不会把不完整区间混入回归。

### 验证

- 新增活动与归档合并、跨周重建、陈旧快照排除和覆盖区间替换业务场景；业务测试增加到 57 个。

## 1.3.3 - 2026-08-26

### 修复

- 断网或旧客户端日志缺少 `service_tier` 时，若 `config.toml` 明确层级早于会话创建且文件没有冲突层级，历史重放可用该配置证据恢复区间。
- 配置恢复的层级显式显示为 `standard(config)` 或 `fast(config)`；配置较新、层级混用或无证据时仍失败关闭。
- 重放算法升级到版本 4，安装后自动追溯重算当前周待归因区间，无需重新采样。

### 验证

- 新增 Standard 配置恢复、Fast 2.5 倍恢复和禁止较新配置反写旧会话三项业务测试；业务测试增加到 54 个。

## 1.3.2 - 2026-08-26

### 修复

- 移除托盘图标左键单击打开快速详情的行为，避免一次双击同时触发快速详情和完整图表两个窗口。
- 快速详情仍可通过托盘右键菜单打开；左键双击继续直达历史曲线和逐点明细。

### 验证

- 新增托盘交互业务测试，覆盖左键单击、左键双击和右键双击三种动作；业务测试增加到 51 个。

## 1.3.1 - 2026-08-26

### 改进

- 总览卡片改为按客户区和 DPI 计算的响应式三列/两列/单列布局，不再使用会在高 DPI 下裁切的固定行高和 AutoSize 百分比表格。
- App Server 连接状态合并为标题区域徽标，总览只保留六张核心指标卡。
- 指标卡增加语义强调色、左侧色条、分组排版和大号主值；修复主题递归刷新覆盖字体、给标签附加白色背景的问题。
- 一级导航改为深色科技侧栏，使用品牌区、矢量图标、宽触控区域和青色选中强调条。
- 双口径金额提升为深色 Hero 卡；已用/剩余增加底部进度指示，辅助指标保持低视觉权重。
- README 改为快速上手页；算法口径和安装排障移入 `docs/`。

### 验证

- 新增 1160×730 真实窗口视口测试，验证六张卡、连接徽标、透明文字背景和大号主值均完整可见。
- 业务测试增加到 50 个，并生成当前 Windows DPI 下的真实总览截图进行人工检查。

## 1.3.0 - 2026-08-26

### 新增

- 总览、历史与图表、设置、诊断四页主窗口。
- Windows 系统显示语言自动检测，以及持久化的简体中文/英文手动覆盖。
- 系统、浅色、深色主题。
- 历史时间范围筛选、图表悬停金额、十字线和双口径系列独立显隐。
- 诊断信息复制、英文 README、MIT License、贡献/安全文档、Windows CI 与 Release 工作流。

### 修复

- 首次从托盘直接打开历史页时，先建立默认总览窗口句柄，再切换目标页，避免 WinForms 布局卡死。
- 中英文界面金额统一显式使用 USD 符号，避免中文区域把 Standard API 等价金额格式化为人民币。
- 设置 schema 2 升级到 schema 3 时保留现有设置和样本，并补齐自动语言、系统主题。
- 详情窗、图表、表格、托盘和设置页统一使用本地时间与当前界面语言。

### 验证

- 49 个业务测试覆盖计价、Fast 倍率、长上下文、重置、历史重放、日志鲁棒性、国际化、主题、四页导航和 100%～200% DPI 布局。
